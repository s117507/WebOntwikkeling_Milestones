<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title><%= title %></title>
</head>
<body>
    <header class="navbar">
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <% if (card) { %>
            <section class="card">
                <h1><%= card.name %></h1>
                <img src="<%= card.imageUrl %>" alt="<%= card.name %>">
                <article class="card-info">
                    <p><%= card.description %></p>
                    <br>
                    <br>
                    <p><strong>Rating:</strong> <%= card.rating %></p>
                    <p><strong>Active:</strong> <%= card.isActive ? 'Yes' : 'No' %></p>
                    <p><strong>Birth Date:</strong> <%= new Date(card.birthDate).toLocaleDateString() %></p>
                    <p><strong>Type:</strong> <%= card.type %></p>
                    <p><strong>Abilities:</strong> <%= card.menu.join(', ') %></p>
                    <p><strong>Artist:</strong> <%= card.artist %></p>
                    <div class="guild-info">
                        <p><strong>Guild Affiliation:</strong></p>
                        <ul>
                            <li><strong>ID:</strong> <%= card.guildAffiliation.id %></li>
                            <li><strong>Name:</strong> <%= card.guildAffiliation.name %></li>
                            <li><strong>Country:</strong> <%= card.guildAffiliation.country %></li>
                        </ul>
                    </div>
                </article>
            </section>
        <% } else { %>
            <p>Card not found.</p>
        <% } %>
    </main>
</body>
</html>
